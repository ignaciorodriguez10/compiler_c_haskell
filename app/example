x = 50;
a = "hola que tal";
y = 15;


if (x >= 5 && x < 100) {
    x = x - 1;
    x = x + 1;
    while (x > 1 && y > 1){
        y = y - 1;
        x = x - 1;
        x = x/2;
        WriteLn(x);
    }
    WriteLn(x);
    WriteLn(y);
}

