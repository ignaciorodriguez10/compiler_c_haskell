int times = 40;
int n1 = 1;
int n2 = 1;

WriteLn(n1);
WriteLn(n2);

while (times <= 0)
{
  times = times - 1;
  int aux = n2;
  int n1 = n2 * 5;
  int n2 = n1 + aux;
}  

WriteLn(aux)
WriteLn(n1)
WriteLn(n2)

WriteLn("Resultados aux - n1 - n2");

